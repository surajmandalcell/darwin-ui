---
title: Select
description: Displays a list of options for the user to pick from.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { SelectPreview } from '../../../components/ComponentPreview';

The Select component offers a native-like experience with enhanced styling and keyboard accessibility.

## Live Preview

<div class="not-content">
  <SelectPreview client:load />
</div>

## Installation

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx shadcn add https://darwin-ui.mandalsuraj.com/registry/select.json
    ```
  </TabItem>
  <TabItem label="Manual">
    ```bash
    npm install @smc/darwin-ui
    ```
  </TabItem>
</Tabs>

## Usage

```tsx
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@smc/darwin-ui';

export function Example() {
  return (
    <Select>
      <SelectTrigger className="w-[180px]">
        <SelectValue placeholder="Theme" />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="light">Light</SelectItem>
        <SelectItem value="dark">Dark</SelectItem>
        <SelectItem value="system">System</SelectItem>
      </SelectContent>
    </Select>
  );
}
```

## API Reference

<div class="overflow-x-auto">
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Default</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`value`</td>
        <td>`string`</td>
        <td>-</td>
        <td>The controlled value of the select item.</td>
      </tr>
      <tr>
        <td>`onValueChange`</td>
        <td>`(value: string) => void`</td>
        <td>-</td>
        <td>Event handler for value changes.</td>
      </tr>
      <tr>
        <td>`disabled`</td>
        <td>`boolean`</td>
        <td>`false`</td>
        <td>Disables interaction.</td>
      </tr>
    </tbody>
  </table>
</div>
