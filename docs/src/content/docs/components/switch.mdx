---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { SwitchPreview } from '../../../components/ComponentPreview';

The Switch component is a beautiful toggle control with spring interactions.

## Live Preview

<div class="not-content">
  <SwitchPreview client:load />
</div>

## Installation

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx shadcn add https://darwin-ui.mandalsuraj.com/registry/switch.json
    ```
  </TabItem>
  <TabItem label="Manual">
    ```bash
    npm install @smc/darwin-ui
    ```
  </TabItem>
</Tabs>

## Usage

```tsx
import { Switch } from '@smc/darwin-ui';

export function Example() {
  return (
    <div className="flex items-center space-x-2">
      <Switch id="airplane-mode" />
      <label htmlFor="airplane-mode">Airplane Mode</label>
    </div>
  );
}
```

## API Reference

<div class="overflow-x-auto">
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Default</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`checked`</td>
        <td>`boolean`</td>
        <td>`false`</td>
        <td>The controlled state.</td>
      </tr>
      <tr>
        <td>`onCheckedChange`</td>
        <td>`(checked: boolean) => void`</td>
        <td>-</td>
        <td>Event handler for state.</td>
      </tr>
      <tr>
        <td>`disabled`</td>
        <td>`boolean`</td>
        <td>`false`</td>
        <td>Disables the switch.</td>
      </tr>
    </tbody>
  </table>
</div>
