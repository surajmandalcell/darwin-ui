---
title: Card
description: A versatile container component with glass-morphism styling, widely used for grouping content.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { CardPreview } from '../../../components/ComponentPreview';

The Card component serves as the fundamental building block for layout, providing a surface to group related content. It features a sophisticated glass material effect that adapts to the background.

## Live Preview

<div class="not-content">
  <CardPreview client:load />
</div>

## Installation

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx shadcn add https://darwin-ui.mandalsuraj.com/registry/card.json
    ```
  </TabItem>
  <TabItem label="Manual">
    ```bash
    npm install @smc/darwin-ui
    ```
  </TabItem>
</Tabs>

## Usage

```tsx title="src/components/ProfileCard.tsx"
import { Card, CardHeader, CardTitle, CardContent, CardFooter, Button } from '@smc/darwin-ui';

export function ProfileCard() {
  return (
    <Card className="w-[350px]">
      <CardHeader>
        <CardTitle>Create project</CardTitle>
        <p className="text-sm text-white/50">Deploy your new project in one-click.</p>
      </CardHeader>
      <CardContent>
        <form>
          {/* Form fields */}
        </form>
      </CardContent>
      <CardFooter className="flex justify-between">
        <Button variant="secondary">Cancel</Button>
        <Button>Deploy</Button>
      </CardFooter>
    </Card>
  );
}
```

## Structure

The Card component typically consists of the following sub-components:

- `Card`: The main container with border and background.
- `CardHeader`: Wrapper for title and description.
- `CardTitle`: The primary heading.
- `CardDescription`: Secondary text for context.
- `CardContent`: The main body area.
- `CardFooter`: Bottom area, usually for actions.

## API Reference

<div class="overflow-x-auto">
  <table>
    <thead>
      <tr>
        <th>Component</th>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`Card`</td>
        <td>`className`</td>
        <td>`string`</td>
        <td>Additional class names for the container.</td>
      </tr>
      <tr>
        <td>`CardHeader`</td>
        <td>`className`</td>
        <td>`string`</td>
        <td>Wrapper for the header section.</td>
      </tr>
      <tr>
        <td>`CardTitle`</td>
        <td>`as`</td>
        <td>`ElementType`</td>
        <td>The semantic HTML heading level (default: `h3`).</td>
      </tr>
      <tr>
        <td>`CardContent`</td>
        <td>`className`</td>
        <td>`string`</td>
        <td>Wrapper for the main content.</td>
      </tr>
    </tbody>
  </table>
</div>

### Accessibility

<div class="checklist">
  <div class="check-item">
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-emerald-500"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
    <span>Appropriate heading hierarchy with `CardTitle`</span>
  </div>
  <div class="check-item">
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-emerald-500"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
    <span>Semantic sectioning</span>
  </div>
</div>

<style>{`
  .checklist {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }
  
  .check-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.95rem;
    color: var(--color-text-muted);
  }
`}</style>
