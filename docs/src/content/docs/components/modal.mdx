---
title: Modal
description: A window overlay that sits on top of a page view.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { ModalPreview } from '../../../components/ComponentPreview';

The Modal component provides a focused environment for user tasks, featuring a backdrop blur and smooth entrance animations.

## Live Preview

<div class="not-content">
  <ModalPreview client:load />
</div>

## Installation

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx shadcn add https://darwin-ui.mandalsuraj.com/registry/dialog.json
    ```
  </TabItem>
  <TabItem label="Manual">
    ```bash
    npm install @smc/darwin-ui
    ```
  </TabItem>
</Tabs>

## Usage

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from '@smc/darwin-ui';

export function Example() {
  return (
    <Dialog>
      <DialogTrigger>Open</DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Are you sure absolutely sure?</DialogTitle>
          <DialogDescription>
            This action cannot be undone. This will permanently delete your account
            and remove your data from our servers.
          </DialogDescription>
        </DialogHeader>
      </DialogContent>
    </Dialog>
  );
}
```

## API Reference

<div class="overflow-x-auto">
  <table>
    <thead>
      <tr>
        <th>Component</th>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`Dialog`</td>
        <td>`open`</td>
        <td>`boolean`</td>
        <td>The controlled open state.</td>
      </tr>
      <tr>
        <td>`Dialog`</td>
        <td>`onOpenChange`</td>
        <td>`(open: boolean) => void`</td>
        <td>Event handler for open state changes.</td>
      </tr>
      <tr>
        <td>`DialogContent`</td>
        <td>`className`</td>
        <td>`string`</td>
        <td>Additional styles for the modal content.</td>
      </tr>
    </tbody>
  </table>
</div>
